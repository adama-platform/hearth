{{<shell}}{{$content}}
<h1>Projects</h1>
<p class="hint">Lists, routines and boards. The community's own are yours to keep; members' own
  are theirs, and this screen deliberately does not open them.</p>
<div class="tools">
  <a class="btn" href="{{tasksUrl}}">Open the community's &rarr;</a>
  <button type="button" class="quiet live" data-live="Go live" data-live-on="Stop">Go live</button>
</div>
<div class="panel" data-panel="{{panelUrl}}">{{{panel}}}</div>
{{/content}}{{/shell}}
