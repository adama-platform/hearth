{{<shell}}{{$content}}
<p class="crumb"><a href="{{backUrl}}">&larr; Survey</a></p>
<h1>Retired questions</h1>
<p class="hint">Retiring a question stops it being asked and stops it counting, immediately. It does
  <strong>not</strong> touch anybody's answers &mdash; those live inside each person's answer sheet,
  and tearing them out is a rewrite of every sheet in the community. That is what committing the
  cleanup does, and why it is a separate button on a separate page.</p>
<div class="panel" data-panel="{{panelUrl}}">{{{panel}}}</div>
{{/content}}{{/shell}}
