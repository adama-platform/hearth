{{<shell}}{{$content}}
<h1>Templates</h1>
<p class="hint">A template wraps content and can declare fields the page editor then asks for. Saving one re-renders every page that uses it.</p>
<div class="doing"><a class="btn" href="{{newUrl}}">{{{plusIcon}}}New template</a></div>
<div class="panel" data-panel="{{panelUrl}}">{{{panel}}}</div>
{{/content}}{{/shell}}
